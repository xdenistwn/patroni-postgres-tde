1. Download minkms tools via cli
2. export MINIO_KMS_ENDPOINT=http://localhost:7373
export MINIO_KMS_API_KEY=k1:sGleLWEYtJsNRbkJz6L8A6lnf8Ov15fq_MK18-8FmnY
minkms stat -k

3. create enclave
minkms add-enclave -k postgres-archive-dev

4. create a policy that will store into identity
minkms add-policy -k postgres-archive-dev-policy ./minio/minkms/policy.dev.json -e postgres-archive-dev

4. add identity for minio aistor
minkms add-identity -k -e postgres-archive-dev

5. get api key for minio aistor
minkms get-identity -k --enclave postgres-archive-dev